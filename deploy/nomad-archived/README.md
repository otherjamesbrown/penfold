# Nomad Deployment (ARCHIVED)

> **This directory is archived.** Nomad has been replaced by native process managers:
> - **launchd** on dev01 (macOS) for the worker
> - **systemd** on dev02 (Linux) for gateway and AI coordinator
>
> See `deploy/launchd/` and `deploy/systemd/` for the active service configuration.
> See `scripts/deploy.sh` for the unified deployment interface.

The HCL job files are preserved here for reference only. Do not use them.
